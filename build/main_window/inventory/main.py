# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Frame, Tk, Canvas, Entry, Text, Button, PhotoImage
from build.main_window.inventory.view_inv.viewProd import ViewInventory
from build.main_window.inventory.update_inv.updateProd import updateInventory
from build.main_window.inventory.add_inv.addprod import addInventory



OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"./assets")

def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)



def inventory():
    Inventory()
    

class Inventory(Frame):
    def __init__(self, parent, controller=None, *args, **kwargs):
        Frame.__init__(self, parent, *args, **kwargs)
        self.parent = parent
        self.selected_rid = None
        
        #self.configure(bg="#FFFFFF")
        
        self.windows = {
         
            "view": ViewInventory(self),
            "edit": updateInventory(self),
            "add": addInventory(self),
       
        }
        
        self.current_window = self.windows["view"]
        self.current_window.place(x=0, y=-4, width=1100.0, height=589.0)
     
        self.current_window.tkraise()

    def navigate(self, name):
        # Hide all screens
        for window in self.windows.values():
            window.place_forget()

        # Show the screen of the button pressed
        self.windows[name].place(x=0, y=-4, width=1100.0, height=589.0)